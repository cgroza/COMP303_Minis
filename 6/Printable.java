// Printable interface for the Printable composite pattern.
public interface Printable
{
    // Required to be implemented by all composites. Needs to return the string
    // representation of the object.
    public String print();
}
